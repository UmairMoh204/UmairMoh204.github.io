const track=document.querySelector('.track'),cards=[...document.querySelectorAll('.card')],counter=document.querySelector('.counter');let current=0;function show(i){current=(i+cards.length)%cards.length;track.style.transform='translateX(-'+current*100+'%)';counter.textContent=(current+1)+' / '+cards.length}document.querySelector('.next').onclick=()=>show(current+1);document.querySelector('.prev').onclick=()=>show(current-1);document.querySelector('.slider').onkeydown=e=>{if(e.key==='ArrowRight')show(current+1);if(e.key==='ArrowLeft')show(current-1)};let x=0;track.addEventListener('touchstart',e=>x=e.touches[0].clientX,{passive:true});track.addEventListener('touchend',e=>{let d=e.changedTouches[0].clientX-x;if(Math.abs(d)>45)show(current+(d<0?1:-1))},{passive:true});